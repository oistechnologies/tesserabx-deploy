# Never commit real secrets.
.env
.env.*
!.env.example

# Upstream checkout / working dir, if it ever lands inside the repo.
# (By default the working dir is /opt/tesserabx, outside this repo.)
/tesserabx/
/upstream/

# Pre-deploy database backups.
.deploy-backups/

# OS / IDE
.DS_Store
.idea/
.vscode/
*.iml
